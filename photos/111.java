 if (Math.abs(x - ((Math.pow(y, number[i]))
                                * (Math.pow(z, number[j]))
                                * (Math.pow(q, number[b]))
                                * (Math.pow(w, number[n])))) < min) {
                            min = Math.abs(x - ((Math.pow(y, number[i]))
                                    * (Math.pow(z, number[j]))
                                    * (Math.pow(q, number[b]))
                                    * (Math.pow(w, number[n]))));
                            dd = number[i];
                            ee = number[j];
                            cc = number[b];
                            jj = number[n];